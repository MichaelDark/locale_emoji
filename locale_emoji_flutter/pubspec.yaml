name: locale_emoji_flutter
description: Convert Locale (i.e. class from dart:ui package) to flag emoji
version: 0.2.0
homepage: https://github.com/MichaelDark/locale_emoji

environment:
  sdk: '>=2.16.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  locale_emoji: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
