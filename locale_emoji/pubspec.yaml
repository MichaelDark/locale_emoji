name: locale_emoji
description: "Convert Locale (language code, script code and country code) to the corresponding Emoji Flag"
version: 1.0.0
homepage: https://github.com/MichaelDark/locale_emoji

environment:
  sdk: '>=2.16.0 <3.0.0'

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
  http: ^0.13.0
